import Giscus from "@giscus/react";
import { FileTree } from "nextra/components";
import {
  Sandpack,
  SandpackProvider,
  SandpackLayout,
  SandpackCodeViewer,
  SandpackPreview,
  UnstyledOpenInCodeSandboxButton,
} from "@codesandbox/sandpack-react";
import { nightOwl } from "@codesandbox/sandpack-themes";

<br/>
# 객체 퀴즈

[배열](../carousel-project-1/13-javascript-11.mdx), [객체](../messenger-project-1/04-data#2-자바스크립트-객체.mdx), 그리고 [함수](../carousel-project-1/11-javascript-9.mdx) 부분을 다시 한번 살펴보고 아래 퀴즈들을 풀어보세요.

> MDN, w3school 등의 문서 또한 참고해보세요.

<Callout emoji="📌">
정확히 어떤 값이 콘솔에 출력되는지 3분 이상 잘 고민해보고, 아래 콘솔 출력문의 주석을 해제하여 정답을 확인해보세요.
</Callout>

<br/>
## 1. 배열

<br/>
### 1-1. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const list = [1, 2, 3];
const result = list.length + 1;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-2. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const result = [1, 2, 3];
result.push(4);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-3. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const result = [1, 2, 3];
result.unshift(0);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-4. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const result = [1, 2, 3];
result.pop();

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-5. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const result = [1, 2, 3];
result.shift();

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-6. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = [1, 2, 3, 4, 5, 2, 10, 13, 2];
const result = arr.indexOf(2);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-6. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = [1, 2, 3, 4, 5, 2, 10, 13, 2];
const result = arr.includes(2, 2);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-7. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = ["010", 1234, 5678];
const result = arr.join("-");

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-8. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = ["셋", "둘", "하나"];
const result = arr.reverse();

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-9. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = [1, 4, 5, 8, 9, 12, 15];
const result = [];

for (let i = 0; i < arr.length; i++) {
  if (i % 2 === 0) {
    result.push(arr[i]);
  }
}

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 1-10. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const arr = [1, 4, 5, 8, 9, 12, 15];
const result = [];

for (let i = arr.length - 1; i >= 0; i--) {
  if (i % 2 === 0) {
    result.push(arr[i]);
  }
}

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
## 2. 객체

<br/>
### 2-1. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const person = {
  name: "leo",
  job: "operations manager",
};

person.years = 3;
person.job = "pm";

const result = person.job;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 2-2. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const employee = {
  name: "dm",
  job: "software engineer",
  years: 1,
};

delete employee.years;

const result = employee.years;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 2-3. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const obj = {
  name: "Vanilla Coding",
  url: "https://www.vanillacoding.co/",
  contact: {
    email: "info@vanillacoding.co",
    phone: "02-6713-7279",
  },
};

const result = obj.contact.email;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 2-4. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const countries = ["South Korea", "Japan", "United States"];

const reservation = {
  name: "ken",
  checkInDate: "2024-01-15",
  checkOutDate: "2024-01-16",
  visitingCountries: countries,
  currentLocation: {
    country: "South Korea",
    city: "Seoul",
  },
};

const result = reservation.visitingCountries[2];

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 2-5. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `const reservation = {
  name: "ken",
  checkInDate: "2024-01-15",
  checkOutDate: "2024-01-16",
  location: {
    country: "South Korea",
    city: "Seoul",
  },
};

const key = "country";
delete reservation.location[key];

const result = reservation.location.country;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
## 3. 함수

<br/>
### 3-1. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function foo() {
  return NaN || 7;
}

const result = foo();

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-2. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function add(x, y) {
  return x + y;
}

const result = add(1, 5);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-3. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function echo(word, times) {
  return word.repeat(times);
}

const result = echo("수박", 3);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-4. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function sayHello() {
  console.log("Hello, World!");
}

const result = sayHello();

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-5. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function isPositive(num) {
  if (num > 0) {
    return true;
  }

  return false;
}

const result = isPositive(1);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-6. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function getGrade(score) {
  if (score >= 90) {
    return "A";
  } else if (score >= 80) {
    return "B";
  } else {
    return "C";
  }
}

const result = getGrade(80);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-7. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function countEven(numbers) {
  let count = 0;

  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 == 0) {
      count++;
    }
  }

  return count;
}

const result = countEven([1, 2, 3, 4, 5, 6]);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-8. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function combineValues(a, b) {
  return a + b;
}

const list = [combineValues("a", "bc"), combineValues("d", "ef")];
const result = list[1];

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-9. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function divideByTwo(num) {
  return num * 2;
}

function square(num) {
  return num * num;
}

function calculate(x) {
  if (x > 0) {
    return square(x);
  } else {
    return divideByTwo(x);
  }
}

const result = calculate(5);

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
### 3-10. 퀴즈

<br/>
<Sandpack
  template="vanilla"
  theme={nightOwl}
  files={{
    "/app.js": {
      code: `function sayWord(word) {
  return word.toLowerCase();
}

const result = \`Hello, \${sayWord("World")}\`;

// console.log(result);`,
      active: true
    },
  }}
  options={{
    showLineNumbers: true,
    editorHeight: "500px",
    editorWidthPercentage: 60,
    initMode: "lazy",
    autoReload: false,
    showConsole: true,
  }}
customSetup={{
    entry: "app.js",
  }}
/>

<br/>
<br/>
<br/>
<Giscus
  repo="crwdu/lms-contents-js101"
  repoId="R_kgDOL2ikqQ"
  category="Q&A"
  categoryId="DIC_kwDOL2ikqc4CgSAu"
  mapping="pathname"
  reactionsEnabled="1"
  strict="1"
  emitMetadata="0"
  inputPosition="top"
  theme="preferred_color_scheme"
  lang="ko"
  loading="lazy"
/>
